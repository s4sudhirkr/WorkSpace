import { PROMOTIONS } from '../../shared/promotions';


// 1st simple reduce
export const Promotions=(state=PROMOTIONS, action)=>
{
    switch(action.type)
    {
        default: 
        return state;
    }
}